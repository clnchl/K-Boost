# Local
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/kboost?schema=public"
PORT=3000

# Render (web service) : lier la base PostgreSQL dans le dashboard → DATABASE_URL
# est injectée automatiquement (URL interne). Depuis ta machine, utiliser
# l’« External Database URL » pour migrate / seed.
