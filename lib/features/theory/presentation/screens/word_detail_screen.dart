import 'package:flutter/material.dart';

class DetailWordScreen extends StatelessWidget {
  
  const DetailWordScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Détail d\'un mot'),
      ),
    );
  }
}