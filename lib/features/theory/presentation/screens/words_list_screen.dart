import 'package:flutter/material.dart';

class WordsListScreen extends StatelessWidget {

  const WordsListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Liste des mots'),
      ),
    );
  }
}
      